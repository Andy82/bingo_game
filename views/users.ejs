<% layout('page') -%>
<% block('title', 'Users'); -%>

<p class="lead">User list</p>

<table>
   <tr>
     <td>Id</td>
     <td>User</td>
   </tr>
<% for(var i=0; i < users.length; i++) { %>
   <tr>
     <td><%= users[i]._id %></td>
     <td><%= users[i].username %></td>
   </tr>
<% } %>
</table>